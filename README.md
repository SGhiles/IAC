# IaC — Mastère 4A

## Observations

Un fichier modifié sans `git add` reste dans le répertoire de travail (working directory).
Il apparaît sous "Changes not staged for commit" dans `git status`,
et n'entrera dans le prochain commit qu'après un `git add`.